{% extends 'base.html.twig' %}

{% block title %}Nouveau mot de passe
{% endblock %}

{% block stylesheets %}

	<link rel="stylesheet" href="{{ asset('css/style.css')}}">
	<link rel="stylesheet" href="{{ asset('css/form.css')}}">

{% endblock %}

{% block body %}
    {% form_theme passwordForm 'forms/fields.twig' %}
    
	{{ form_start(passwordForm, { 'attr' : { 'class': 'text_light' } }) }}
	<div class="form-content">
        <h1>Nouveau mot de passe</h1>

        <div class="inputs">

            {{ form_row(passwordForm.plainPassword) }}
        </div>
        <button type="submit" class="button">Enregistrer</button>
            <div class="top-separator">
            <a class="link-anim" href="{{path('app_login')}}">Revenir à la page de connexion</a>
        </div>
    </div>
	{{ form_end(passwordForm) }}



{% endblock %}
