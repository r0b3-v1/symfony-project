<header>
	<nav>
		<div class="left">
			<div class="logo">
				<img src="{{asset('images/static/logo.png')}}" alt="logo">
			</div>
			<a class='nav_link' href="{{path('app_home')}}">Accueil</a>
			<a class='nav_link' href="#">Rechercher</a>
			{% if is_granted('ROLE_USER') %}
				<a class='nav_link' href="{{ path('app_upload')}}">Upload</a>
			{% endif %}

		</div>
		<div class="right">
			{% if not(is_granted('ROLE_USER')) %}
				<a class="nav_link" href="{{path('app_login')}}">Se Connecter/S'inscrire</a>
			{% else %}

				<span class="user">
					<a href="{{ path('app_profile_infos',{username: app.user.username}) }}">
					Bienvenue
					{{app.user.username}}!
						{% if not(app.user.avatar)%}
							<img src="{{ asset('images/static/avatar.png') }}" alt="avatar">
						{% endif %}
					</a>
				</span>

				<a class="nav_link" href="{{path('app_logout')}}">Se déconnecter</a>
			{% endif %}
		</div>
	</nav>
</header>
